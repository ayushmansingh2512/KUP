# API Base URL for the hosted backend.
# In local development, leave this blank to automatically fall back to the Vite dev server proxy (localhost:8080).
# In production, set this to your hosted backend's URL.
# e.g., VITE_API_BASE_URL=https://your-backend-app.onrender.com
VITE_API_BASE_URL=
